---
phase: quick
plan: 260616-ty3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content.config.ts
  - src/components/EventCard.astro
  - src/pages/index.astro
  - src/content/events/op-primate-protocol.md
autonomous: true
requirements:
  - QUICK-DEV-BRANCH
  - QUICK-DYNAMIC-STATUS
must_haves:
  truths:
    - "A `dev` branch exists, branched from `main`, and is the active checked-out branch"
    - "An event dated in the future renders an UPCOMING badge"
    - "An event dated today renders an OP IN PROGRESS badge"
    - "An event dated in the past renders a PREVIOUS OPERATION badge"
    - "An event with a manual cancelled flag renders a CANCELLED badge regardless of date"
    - "`npm run build` (astro check + astro build) succeeds with no type errors"
  artifacts:
    - path: "src/content.config.ts"
      provides: "Event schema where date drives status; cancellation is a manual boolean override"
      contains: "cancelled"
    - path: "src/components/EventCard.astro"
      provides: "Badge rendering for upcoming / in-progress / past / cancelled derived states"
      contains: "in-progress"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/EventCard.astro"
      via: "computed status prop passed per event"
      pattern: "status="
    - from: "src/components/EventCard.astro"
      to: "badgeConfig"
      via: "status keys badge label/classes lookup"
      pattern: "badgeConfig"
---

<objective>
Create a `dev` working branch and replace hardcoded event status with build-time
status derived from each event's date: future = upcoming, today = op in progress,
past = previous operation. A manual `cancelled` flag remains as an override that
date logic cannot infer.

Purpose: Statuses stay accurate without manual edits as event dates pass. Dev work
moves onto a `dev` branch per the developer's workflow request.
Output: `dev` branch; updated content schema, EventCard component, index page, and
the sample event file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/content.config.ts
@src/components/EventCard.astro
@src/pages/index.astro
@src/content/events/op-primate-protocol.md

Color tokens available in src/styles/global.css:
`--color-accent` (#8aad3a green), `--color-badge-completed` (#3f3f46 gray),
`--color-destructive` (#7f2e2e red), used via Tailwind classes
`bg-accent` / `bg-badge-completed` / `bg-destructive`.

Note: this is a static Astro site, so `new Date()` evaluates at build time. Status
is correct as of the last build/deploy, which is the intended behavior.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dev branch from main</name>
  <files>(git operation, no file changes)</files>
  <action>From the repo root, create and check out a new `dev` branch off the current
  `main`: `git checkout -b dev`. This is the only git task; all subsequent file edits
  in this plan land on `dev`. Do not push — leave pushing to the developer.</action>
  <verify>
    <automated>git -C /home/michael/git/deathmonkeys rev-parse --abbrev-ref HEAD | grep -qx dev</automated>
  </verify>
  <done>`git rev-parse --abbrev-ref HEAD` reports `dev`.</done>
</task>

<task type="auto">
  <name>Task 2: Make status date-derived in schema and content</name>
  <files>src/content.config.ts, src/content/events/op-primate-protocol.md</files>
  <action>In src/content.config.ts, change the events schema so date drives status and
  cancellation is a manual override. Remove the required `status: z.enum(['upcoming',
  'completed', 'cancelled'])` field. Add `cancelled: z.boolean().optional().default(false)`.
  Keep `date: z.coerce.date()` as the source of truth for derived status.
  In src/content/events/op-primate-protocol.md, remove the `status: upcoming` frontmatter
  line (no longer a valid field). Do not add a `cancelled` line since this event is not
  cancelled and the field defaults to false.</action>
  <verify>
    <automated>cd /home/michael/git/deathmonkeys && npx astro check 2>&1 | grep -iq "0 errors" && grep -q "cancelled" src/content.config.ts && ! grep -q "status:" src/content/events/op-primate-protocol.md</automated>
  </verify>
  <done>Schema has no `status` field, has optional `cancelled` boolean; sample event
  has no `status` frontmatter; `astro check` passes with 0 errors.</done>
</task>

<task type="auto">
  <name>Task 3: Derive status from date and render new badges</name>
  <files>src/components/EventCard.astro, src/pages/index.astro</files>
  <action>In src/components/EventCard.astro: change the Props interface to take
  `cancelled: boolean` instead of `status`, and compute the derived status inside the
  component frontmatter from `date` and `cancelled`. Logic, comparing date-only
  (zero out time on both sides): if `cancelled` -> 'cancelled'; else compare event
  date to today -> future = 'upcoming', same calendar day = 'in-progress',
  past = 'past'. Build today via `new Date()` and normalize with
  `setHours(0,0,0,0)` on copies of both dates to avoid time-of-day skew, then compare
  with getTime(). Expand badgeConfig to four keys:
  upcoming -> label 'UPCOMING', classes 'bg-accent text-text-primary';
  in-progress -> label 'OP IN PROGRESS', classes 'bg-accent text-text-primary animate-pulse' (or another clearly-active treatment using existing tokens);
  past -> label 'PREVIOUS OPERATION', classes 'bg-badge-completed text-text-secondary';
  cancelled -> label 'CANCELLED', classes 'bg-destructive text-text-primary'.
  Look the badge up by the computed status.
  In src/pages/index.astro: the events are spread as `{...event.data}` into EventCard.
  Since `cancelled` now defaults via schema it is present on `event.data`; confirm the
  spread still type-checks (EventCard no longer expects `status`). No status sorting
  change needed; existing date sort is retained.</action>
  <verify>
    <automated>cd /home/michael/git/deathmonkeys && npm run build 2>&1 | tail -5 | grep -iq "complete\|built" && grep -q "in-progress" src/components/EventCard.astro && grep -q "PREVIOUS OPERATION" src/components/EventCard.astro && ! grep -q "status:" src/components/EventCard.astro</automated>
  </verify>
  <done>EventCard computes status from date+cancelled, renders UPCOMING /
  OP IN PROGRESS / PREVIOUS OPERATION / CANCELLED badges; `npm run build` succeeds.</done>
</task>

</tasks>

<verification>
- `git rev-parse --abbrev-ref HEAD` == `dev`
- `npm run build` (runs `astro check && astro build`) exits 0
- Manual spot check: temporarily set the sample event date to a past date and a
  today date, rebuild, and confirm badge changes (revert date after).
</verification>

<success_criteria>
- `dev` branch created from `main` and checked out
- Event status no longer hardcoded; derived at build time from date
- Three date-based states render: UPCOMING (future), OP IN PROGRESS (today),
  PREVIOUS OPERATION (past)
- CANCELLED override still available via manual `cancelled: true` frontmatter
- `npm run build` passes clean (astro check + build)
</success_criteria>

<output>
Create `.planning/quick/260616-ty3-create-dev-branch-and-implement-dynamic-/260616-ty3-SUMMARY.md` when done
</output>
