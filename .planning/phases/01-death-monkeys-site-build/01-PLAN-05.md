---
id: "01-5"
plan: "5"
phase: "01"
title: "Gallery Detail + QA"
wave: 5
depends_on: ["01-4"]
files_modified:
  - src/pages/gallery/[id].astro
files_modified_list: [src/pages/gallery/[id].astro]
type: execute
autonomous: true
requirements: [PAGE-06, QA-01, QA-02]
must_haves:
  truths:
    - "Each gallery seed entry has a detail page at /gallery/[id] rendering its full AAR markdown body and photos list"
    - "The detail route uses entry.id (NOT entry.slug) and render() from astro:content"
    - "astro check exits with zero type errors across the whole project"
    - "npm run build succeeds and dist/ contains HTML for all six routes including each gallery detail page"
  artifacts:
    - path: "src/pages/gallery/[id].astro"
      provides: "Dynamic gallery detail route with getStaticPaths + render"
      contains: "getStaticPaths"
  key_links:
    - from: "src/pages/gallery/[id].astro"
      to: "getCollection('gallery')"
      via: "getStaticPaths enumerating entries by id"
      pattern: "getStaticPaths"
    - from: "src/pages/gallery/[id].astro"
      to: "astro:content render()"
      via: "standalone render(entry) for AAR body"
      pattern: "render\\("
---

<objective>
Build the dynamic gallery detail route `/gallery/[id].astro` (via `getStaticPaths` + standalone `render(entry)`) so every gallery seed entry has a full AAR detail page rendering its markdown body and photos list, then run the final QA gates. End state: `astro check` is zero-error across the whole project and `npm run build` produces a valid `dist/` containing HTML for all six routes — completing the Walking Skeleton end-to-end.

This closes the data-layer loop (read → list → detail) and proves the full static stack builds and type-checks.

Purpose: Final vertical slice (detail pages) + the QA gates that gate the phase as shippable.
Output: `src/pages/gallery/[id].astro` and verified clean build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-death-monkeys-site-build/01-UI-SPEC.md
@.planning/phases/01-death-monkeys-site-build/SKELETON.md
@.planning/phases/01-death-monkeys-site-build/01-2-SUMMARY.md
@.planning/phases/01-death-monkeys-site-build/01-4-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the /gallery/[id].astro dynamic detail route</name>
  <files>src/pages/gallery/[id].astro</files>
  <read_first>
    - CLAUDE.md (section 3 — Content Layer API: `entry.id` not `entry.slug`, standalone `render(entry)` from `astro:content` not `entry.render()`, `getStaticPaths` for static dynamic routes)
    - src/content.config.ts (gallery schema — title/date/summary/eventRef?/photos?)
    - src/components/GalleryCard.astro (confirm the link target format `/gallery/${id}` so route params match)
    - .planning/phases/01-death-monkeys-site-build/01-UI-SPEC.md (Color/Typography for detail page — surface bg, heading scale, muted text; photos list layout)
  </read_first>
  <action>
    Create `src/pages/gallery/[id].astro`:
    - Frontmatter: `import { getCollection, render } from 'astro:content';` and `import BaseLayout from '../../layouts/BaseLayout.astro';`.
    - `export async function getStaticPaths() { const entries = await getCollection('gallery'); return entries.map((entry) => ({ params: { id: entry.id }, props: { entry } })); }` — use `entry.id` for the param (CLAUDE.md: NOT `entry.slug`), matching the `/gallery/${id}` link from GalleryCard.
    - In the component body: `const { entry } = Astro.props;` then `const { Content } = await render(entry);` (standalone `render()` from `astro:content`, NOT `entry.render()`).
    - Wrap in `<BaseLayout title={entry.data.title}>`.
    - Render the AAR detail: the `title` (heading), formatted `date`, optional `eventRef`, the `summary`, the full markdown body via `<Content />`, and a photos list — iterate `entry.data.photos` (guard with `entry.data.photos?.map(...)`) rendering each as an `<img>` with placeholder URL; if `photos` is undefined, render nothing for that block (one seed entry omits photos to exercise the optional path).
  </action>
  <verify>
    <automated>cd /home/michael/git/deathmonkeys && test -f "src/pages/gallery/[id].astro" && grep -q "getStaticPaths" "src/pages/gallery/[id].astro" && grep -q "entry.id" "src/pages/gallery/[id].astro" && ! grep -q "entry.slug" "src/pages/gallery/[id].astro" && grep -q "render(entry)" "src/pages/gallery/[id].astro" && ! grep -q "entry.render()" "src/pages/gallery/[id].astro" && grep -q "<Content" "src/pages/gallery/[id].astro" && echo 'detail route OK'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/pages/gallery/[id].astro` and exports `getStaticPaths` mapping over `getCollection('gallery')` using `entry.id` as the `params.id`.
    - Uses standalone `render(entry)` from `astro:content` and renders `<Content />`; contains NO `entry.slug` and NO `entry.render()`.
    - Guards `entry.data.photos` with optional access (`photos?.`) so the no-photos seed entry renders without error.
    - Wraps content in `<BaseLayout title={entry.data.title}>`.
  </acceptance_criteria>
  <done>Every gallery entry has a working detail page rendering its AAR body and photos list.</done>
</task>

<task type="auto">
  <name>Task 2: Final QA gates — astro check clean + full build to dist/</name>
  <files>src/pages/gallery/[id].astro</files>
  <read_first>
    - CLAUDE.md (section 6 — `build` = `astro check && astro build`; Constraints — `astro check` clean, `npm run build` must succeed)
    - .planning/ROADMAP.md (Phase 1 success criteria — all six routes render, check zero errors, build succeeds)
    - .planning/phases/01-death-monkeys-site-build/SKELETON.md (Stack Touched checklist — confirm every box is exercised by build)
  </read_first>
  <action>
    Run the full QA gate sequence and confirm the Walking Skeleton is complete (no source edits expected here unless a gate fails; if `astro check` reports an error, fix the offending file and re-run):
    - `npx astro sync` (regenerate types after the new route).
    - `npx astro check` → MUST exit 0 (QA-01).
    - `npm run build` (runs `astro check && astro build`) → MUST succeed (QA-02).
    - Confirm `dist/` contains HTML for all six routes: `/`, `/about`, `/events`, `/roster`, `/gallery`, and one `/gallery/<id>/` directory per gallery seed entry.
    - Confirm there are no broken internal links: every nav/section link target resolves to a built route (home→sections, GalleryCard→detail).
  </action>
  <verify>
    <automated>cd /home/michael/git/deathmonkeys && npx astro sync && npx astro check 2>&1 | tail -5</automated>
    <automated>cd /home/michael/git/deathmonkeys && npm run build 2>&1 | tail -10 && for f in dist/index.html dist/about/index.html dist/events/index.html dist/roster/index.html dist/gallery/index.html; do test -f "$f" || { echo "missing $f"; exit 1; }; done && DETAIL=$(find dist/gallery -mindepth 2 -name index.html | wc -l) && test "$DETAIL" -ge 2 && echo "all six route types built; $DETAIL gallery detail pages"</automated>
  </verify>
  <acceptance_criteria>
    - `npx astro check` exits 0 with zero type errors across the project (QA-01).
    - `npm run build` exits 0 (QA-02).
    - `dist/` contains `index.html` for `/`, `/about`, `/events`, `/roster`, and `/gallery`.
    - `dist/gallery/` contains at least 2 nested detail `index.html` files (one per gallery seed entry with detail content).
  </acceptance_criteria>
  <done>Whole project type-checks clean and builds to a valid dist/ with all six route types — Walking Skeleton complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gallery markdown body → rendered HTML | AAR body content rendered via `<Content />` into the detail page |
| Route param `id` → static path | Collection entry id becomes the URL segment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | XSS via markdown AAR body (`<Content />`) | mitigate | Astro renders collection markdown through its sanitizing pipeline; content is first-party author-written seed, validated in Plan 1.2. No `set:html` on untrusted input. |
| T-05-02 | Spoofing | Mismatched `id` causing 404 detail links | mitigate | `getStaticPaths` enumerates the same `getCollection('gallery')` ids the GalleryCard links to; build fails / produces no orphan link. Verified by counting built detail pages in QA gate. |
| T-05-03 | Tampering | Build supply chain (final build step) | mitigate | Build runs only against the pinned, lockfile-locked dependency tree from Plan 1.1; no new packages introduced in this plan. |
</threat_model>

<verification>
- `cd /home/michael/git/deathmonkeys && npx astro check` → exits 0 (QA-01).
- `cd /home/michael/git/deathmonkeys && npm run build` → succeeds (QA-02).
- `test -f "src/pages/gallery/[id].astro" && grep -q "getStaticPaths" "src/pages/gallery/[id].astro"` → detail route present.
- `! grep -q "entry.slug" "src/pages/gallery/[id].astro"` → uses entry.id per v5.
- `find dist/gallery -mindepth 2 -name index.html | wc -l` → ≥ 2 detail pages.
</verification>

<success_criteria>
1. Each gallery seed entry has a detail page at `/gallery/[id]` rendering its full AAR content and photos list (uses `entry.id`, not `entry.slug`; standalone `render()`).
2. `astro check` exits with zero type errors across the entire project.
3. `npm run build` succeeds and `dist/` contains HTML for all six page routes including gallery detail pages.
</success_criteria>

<output>
Create `.planning/phases/01-death-monkeys-site-build/01-5-SUMMARY.md` when done.
</output>
