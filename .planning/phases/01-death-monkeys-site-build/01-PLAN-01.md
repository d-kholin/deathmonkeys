---
id: "01-1"
plan: "1"
phase: "01"
title: "Scaffold + Config"
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - tsconfig.json
  - src/styles/global.css
  - src/pages/index.astro
files_modified_list: [package.json, astro.config.mjs, tsconfig.json, src/styles/global.css, src/pages/index.astro]
type: execute
wave_note: "no dependencies — first plan"
autonomous: true
requirements: [FOUND-01, FOUND-02]
must_haves:
  truths:
    - "astro dev starts and serves a skeleton page at localhost without errors"
    - "astro check passes with zero type errors on the skeleton project"
    - "The skeleton page background renders near-black #0a0a0a, proving Tailwind v4 is wired via @tailwindcss/vite"
  artifacts:
    - path: "package.json"
      provides: "Pinned Astro v5 + Tailwind v4 deps and check/build scripts"
      contains: "astro check && astro build"
    - path: "astro.config.mjs"
      provides: "Static output + Tailwind v4 Vite plugin"
      contains: "output: 'static'"
    - path: "src/styles/global.css"
      provides: "@import tailwindcss + @theme design tokens"
      contains: "@theme"
    - path: "src/pages/index.astro"
      provides: "Skeleton page proving the stack renders"
  key_links:
    - from: "astro.config.mjs"
      to: "@tailwindcss/vite"
      via: "vite.plugins array"
      pattern: "tailwindcss\\(\\)"
    - from: "src/pages/index.astro"
      to: "src/styles/global.css"
      via: "import in frontmatter"
      pattern: "import.*global.css"
---

<objective>
Scaffold a greenfield Astro v5 static project configured with Tailwind v4 (via `@tailwindcss/vite`), TypeScript strict mode, and the dark-tactical `@theme` design tokens. End state: a skeleton `index.astro` renders in the browser on a near-black `#0a0a0a` background and `astro check` passes with zero type errors.

This is the first slice of the Walking Skeleton: it proves the build toolchain (Astro + Tailwind v4 + TS) compiles and serves a real page end-to-end before any content or components are added.

Purpose: Establish the buildable foundation every later plan depends on. No layout, components, or collections yet — just a proven, type-clean stack.
Output: `package.json`, `astro.config.mjs`, `tsconfig.json`, `src/styles/global.css`, `src/pages/index.astro`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-death-monkeys-site-build/01-UI-SPEC.md
@.planning/phases/01-death-monkeys-site-build/SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Astro v5 + Tailwind v4 project with pinned deps and scripts</name>
  <files>package.json, astro.config.mjs, tsconfig.json</files>
  <read_first>
    - CLAUDE.md (sections 1, 2, 4, 5, 6, 7 — pinned versions, astro.config shape, tsconfig presets, Node version, scripts, v5 deprecations)
    - .planning/phases/01-death-monkeys-site-build/SKELETON.md (architectural decisions table — confirms output:'static', @tailwindcss/vite, directory layout)
  </read_first>
  <action>
    Create a minimal Astro v5 project by hand (do NOT run `npm create astro` interactively — write files directly, then run `npm install`).

    `package.json`:
    - `"type": "module"`.
    - dependencies pinned exactly per CLAUDE.md section 1: `astro@^5.18.2`, `@tailwindcss/vite@^4.3.1`, `tailwindcss@^4.3.1`.
    - devDependencies: `@astrojs/check@^0.9.9`, `typescript@^5`.
    - scripts: `"dev": "astro dev"`, `"build": "astro check && astro build"`, `"preview": "astro preview"`, `"check": "astro check"`, `"sync": "astro sync"`.
    - Do NOT add `@astrojs/tailwind` (deprecated for v4 — CLAUDE.md section 7).

    `astro.config.mjs`:
    - `import { defineConfig } from 'astro/config';` and `import tailwindcss from '@tailwindcss/vite';`.
    - `export default defineConfig({ output: 'static', vite: { plugins: [tailwindcss()] } });`.
    - Do NOT use `output: 'hybrid'` (removed in v5) and do NOT place Tailwind in `integrations`.

    `tsconfig.json`:
    - `"extends": "astro/tsconfigs/strict"` (the recommended strict preset, CLAUDE.md section 4).
    - `"include": [".astro/types.d.ts", "**/*"]`, `"exclude": ["dist"]` (CLAUDE.md section 7 — `.astro/types.d.ts` supersedes `src/env.d.ts`).

    Run `npm install` to materialize `node_modules` and `package-lock.json`.
  </action>
  <verify>
    <automated>cd /home/michael/git/deathmonkeys && node -e "const p=require('./package.json'); const d={...p.dependencies,...p.devDependencies}; if(!/^\^5\./.test(d.astro)) throw new Error('astro not pinned ^5'); if(d['@astrojs/tailwind']) throw new Error('forbidden @astrojs/tailwind present'); if(p.scripts.build!=='astro check && astro build') throw new Error('build script wrong'); console.log('package.json OK')"</automated>
    <automated>cd /home/michael/git/deathmonkeys && grep -v '^[[:space:]]*//' astro.config.mjs | grep -q "output: 'static'" && grep -q 'tailwindcss()' astro.config.mjs && echo 'astro.config OK'</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies include `astro` matching `^5.`, and `@tailwindcss/vite` + `tailwindcss` matching `^4.`.
    - `package.json` has NO `@astrojs/tailwind` dependency anywhere.
    - `package.json` `scripts.build` is exactly `astro check && astro build`.
    - `astro.config.mjs` contains `output: 'static'` and `tailwindcss()` inside `vite.plugins`, and contains NO `output: 'hybrid'` and NO `integrations` Tailwind entry.
    - `tsconfig.json` extends `astro/tsconfigs/strict`.
    - `npm install` completes and `node_modules/astro` exists.
  </acceptance_criteria>
  <done>Project deps installed; config files match the CLAUDE.md v5 + Tailwind v4 contract.</done>
</task>

<task type="auto">
  <name>Task 2: Global CSS with @theme tokens and skeleton index page</name>
  <files>src/styles/global.css, src/pages/index.astro</files>
  <read_first>
    - CLAUDE.md (section 2 — Tailwind v4 CSS-first, `@import "tailwindcss"`, `@theme`)
    - .planning/phases/01-death-monkeys-site-build/01-UI-SPEC.md (Tailwind v4 Token Declarations block — exact `@theme` custom properties and Google Fonts import line; Color table)
    - src/styles/global.css (current state — will not exist yet; create it)
  </read_first>
  <action>
    Create `src/styles/global.css` exactly following the UI-SPEC "Tailwind v4 Token Declarations" section:
    - Line 1: the Google Fonts `@import url('https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;700&display=swap');` — MUST be the first line (CSS requires `@import` before other rules; UI-SPEC notes it goes "before `@import "tailwindcss"`").
    - Then `@import "tailwindcss";`.
    - Then a `@theme { }` block declaring ALL tokens from the UI-SPEC block verbatim: colors (`--color-surface: #0a0a0a;`, `--color-surface-raised: #141414;`, `--color-surface-inactive: #1f1f1f;`, `--color-accent: #6b7c4a;`, `--color-destructive: #7f2e2e;`, `--color-badge-completed: #3f3f46;`, `--color-text-primary: #f4f4f5;`, `--color-text-secondary: #a1a1aa;`, `--color-text-muted: #6b7280;`, `--color-border: #27272a;`), fonts (`--font-family-heading: 'JetBrains Mono', monospace;`, `--font-family-body: ui-sans-serif, system-ui, sans-serif;`), spacing (`--spacing-xs` 4px through `--spacing-3xl` 64px), and `--nav-height: 56px;`.
    - Add a base rule setting `body { background-color: var(--color-surface); color: var(--color-text-primary); font-family: var(--font-family-body); }` so the near-black background is provable.

    Create `src/pages/index.astro` as a SKELETON page (no BaseLayout yet — that arrives in Plan 1.3):
    - Frontmatter imports the global CSS: `import '../styles/global.css';`.
    - A minimal full HTML document (`<html lang="en">`, `<head>` with `<meta charset>`, `<title>Death Monkeys</title>`, `<body>` with a single `<h1>DEATH MONKEYS</h1>`).
    - This is intentionally throwaway scaffolding; Plan 1.4 replaces it with the real home page.
  </action>
  <verify>
    <automated>cd /home/michael/git/deathmonkeys && grep -q '@import "tailwindcss"' src/styles/global.css && grep -q '@theme' src/styles/global.css && grep -q -- '--color-surface: #0a0a0a' src/styles/global.css && grep -q "JetBrains+Mono" src/styles/global.css && echo 'global.css OK'</automated>
    <automated>cd /home/michael/git/deathmonkeys && grep -q "global.css" src/pages/index.astro && npx astro sync && npx astro check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/styles/global.css` first non-empty line is the JetBrains Mono Google Fonts `@import url(...)`.
    - `src/styles/global.css` contains `@import "tailwindcss";` and a `@theme {` block.
    - `@theme` block contains `--color-surface: #0a0a0a;` and `--color-accent: #6b7c4a;` and `--nav-height: 56px;`.
    - `src/pages/index.astro` imports `../styles/global.css`.
    - `npx astro check` exits 0 (zero type errors).
    - `npm run build` produces `dist/index.html` whose inline/linked CSS resolves `--color-surface` to `#0a0a0a` (background token present in output).
  </acceptance_criteria>
  <done>Skeleton page renders on near-black background; `astro check` passes clean; Tailwind v4 confirmed wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local build | Third-party package code executes during install/build |
| Google Fonts CDN → browser | External `@import` of font CSS at render time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm dependency supply chain | mitigate | Pin exact-minor versions per CLAUDE.md section 1 (`astro@^5.18.2`, `tailwindcss@^4.3.1`, `@tailwindcss/vite@^4.3.1`, `@astrojs/check@^0.9.9`); commit `package-lock.json` to lock the resolved tree. |
| T-01-02 | Spoofing | Google Fonts external CSS `@import` | accept | First-party Google Fonts CDN over HTTPS only; no PII sent; font CSS cannot execute JS. Low-value static site. |
| T-01-03 | Elevation | Build-time arbitrary code (postinstall scripts) | mitigate | All packages are official Astro/Tailwind first-party packages documented in CLAUDE.md authoritative setup guide — not [ASSUMED]/[SUS]/[SLOP]. Versions pinned; lockfile committed. |
| T-01-SC | Tampering | npm installs | mitigate | Packages (`astro`, `tailwindcss`, `@tailwindcss/vite`, `typescript`, `@astrojs/check`) are vendor-official, version-pinned in CLAUDE.md. No [ASSUMED]/[SUS] packages → no blocking human checkpoint required. |
</threat_model>

<verification>
- `cd /home/michael/git/deathmonkeys && npx astro check` → exits 0, zero errors.
- `cd /home/michael/git/deathmonkeys && npm run build` → succeeds, `dist/index.html` exists.
- `grep -q "output: 'static'" astro.config.mjs` → match.
- `grep -q 'tailwindcss()' astro.config.mjs` → match (Tailwind in vite.plugins).
- `! grep -q '@astrojs/tailwind' package.json` → no forbidden integration.
</verification>

<success_criteria>
1. `npx astro dev` starts without errors and serves the skeleton page at localhost.
2. `astro check` passes with zero type errors.
3. The skeleton page background resolves to `#0a0a0a` via the `--color-surface` token, confirming Tailwind v4 is wired through `@tailwindcss/vite`.
</success_criteria>

<output>
Create `.planning/phases/01-death-monkeys-site-build/01-1-SUMMARY.md` when done.
</output>
