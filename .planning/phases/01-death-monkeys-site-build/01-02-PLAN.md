---
id: "01-02"
plan: "2"
phase: "01"
title: "Content Collections"
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/content.config.ts
  - src/content/events/op-primate-protocol.md
  - src/content/events/black-banana-engagement.md
  - src/content/events/op-canopy-breach.md
  - src/content/roster/nakunga.md
  - src/content/roster/gravedigger.md
  - src/content/roster/jackdaw.md
  - src/content/gallery/aar-primate-protocol.md
  - src/content/gallery/aar-black-banana.md
  - src/content/gallery/aar-canopy-breach.md
files_modified_list: [src/content.config.ts, src/content/events/, src/content/roster/, src/content/gallery/]
type: execute
autonomous: true
requirements: [FOUND-03, CONTENT-01, CONTENT-02, CONTENT-03, CONTENT-04]
must_haves:
  truths:
    - "src/content.config.ts exists at the root of src/ (NOT inside src/content/)"
    - "astro sync generates types for events, roster, and gallery in .astro/types.d.ts"
    - "Each collection has 2-3 markdown seed files whose frontmatter satisfies the zod schema without coercion errors"
    - "astro check passes with all collection types resolving"
  artifacts:
    - path: "src/content.config.ts"
      provides: "events, roster, gallery collections defined with glob() loader + zod schemas"
      contains: "defineCollection"
    - path: "src/content/events/op-primate-protocol.md"
      provides: "Seed event satisfying CONTENT-01 schema"
    - path: "src/content/roster/nakunga.md"
      provides: "Anchor roster entry (callsign Nakunga / Naku) satisfying CONTENT-02"
    - path: "src/content/gallery/aar-primate-protocol.md"
      provides: "Seed AAR satisfying CONTENT-03 schema"
  key_links:
    - from: "src/content.config.ts"
      to: "src/content/events"
      via: "glob loader base/pattern"
      pattern: "glob\\("
    - from: "src/content.config.ts"
      to: "astro/zod"
      via: "schema import"
      pattern: "from 'astro:content'"
---

<objective>
Define all three content collections (`events`, `roster`, `gallery`) in `src/content.config.ts` using the Astro v5 Content Layer API (`glob()` loader + zod schemas), and seed each with 2–3 realistic markdown entries carrying the Death Monkeys brand voice. End state: `astro sync` generates collection types and `astro check` resolves every collection type cleanly.

This slice makes the data layer real: subsequent pages (Plans 1.4/1.5) read these collections via `getCollection`/`render`. No UI yet — this is the typed data foundation, proven by `astro check`.

Purpose: Establish the typed content backbone the events/roster/gallery pages consume.
Output: `src/content.config.ts` and 9 seed markdown files (3 per collection).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-death-monkeys-site-build/01-CONTEXT.md
@.planning/phases/01-death-monkeys-site-build/SKELETON.md
@.planning/phases/01-death-monkeys-site-build/01-1-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define events, roster, gallery collections in src/content.config.ts</name>
  <files>src/content.config.ts</files>
  <read_first>
    - CLAUDE.md (section 3 — Content Layer API: `src/content.config.ts` location, `loader: glob()`, `z` from `astro:content`/`astro/zod`, entry IDs from file paths)
    - .planning/REQUIREMENTS.md (CONTENT-01, CONTENT-02, CONTENT-03 — exact field names, types, enums, optionality)
    - .planning/phases/01-death-monkeys-site-build/01-1-SUMMARY.md (confirm scaffold state from Plan 1.1)
  </read_first>
  <action>
    Create `src/content.config.ts` at the ROOT of `src/` (NOT `src/content/config.ts` — CLAUDE.md section 3).
    - `import { defineCollection, z } from 'astro:content';` and `import { glob } from 'astro/loaders';`.
    - Each collection uses `loader: glob({ pattern: '**/*.md', base: './src/content/<name>' })`.
    - Do NOT use `type: 'content'` (deprecated v2 API).

    `events` schema (CONTENT-01): `title: z.string()`, `date: z.coerce.date()`, `location: z.string()`, `description: z.string()`, `status: z.enum(['upcoming', 'completed', 'cancelled'])`.

    `roster` schema (CONTENT-02): `callsign: z.string()`, `realName: z.string().optional()`, `role: z.string()`, `loadout: z.string()`, `joinedDate: z.coerce.date().optional()`, `active: z.boolean()`.

    `gallery` schema (CONTENT-03): `title: z.string()`, `date: z.coerce.date()`, `eventRef: z.string().optional()`, `summary: z.string()`, `photos: z.array(z.string()).optional()`.

    Export `const collections = { events, roster, gallery };`.
  </action>
  <verify>
    <automated>cd /home/michael/git/deathmonkeys && test -f src/content.config.ts && ! test -f src/content/config.ts && grep -q "glob(" src/content.config.ts && grep -q "z.enum(\['upcoming', 'completed', 'cancelled'\])" src/content.config.ts && grep -q "export const collections" src/content.config.ts && echo 'config OK'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/content.config.ts`; file does NOT exist at `src/content/config.ts`.
    - File imports `glob` from `astro/loaders` and uses `loader: glob(...)` for all three collections (no `type: 'content'`).
    - `events` schema declares the `status` enum with exactly `upcoming`, `completed`, `cancelled`.
    - `roster` schema marks `realName`, `joinedDate` as `.optional()` and `active` as `z.boolean()`.
    - `gallery` schema marks `eventRef`, `photos` as `.optional()` with `photos` as `z.array(z.string())`.
    - `export const collections` includes `events`, `roster`, `gallery`.
  </acceptance_criteria>
  <done>All three collections defined with v5 glob loader + zod schemas; exported.</done>
</task>

<task type="auto">
  <name>Task 2: Seed 3 events, 3 roster, 3 gallery markdown entries with Death Monkeys voice</name>
  <files>src/content/events/op-primate-protocol.md, src/content/events/black-banana-engagement.md, src/content/events/op-canopy-breach.md, src/content/roster/nakunga.md, src/content/roster/gravedigger.md, src/content/roster/jackdaw.md, src/content/gallery/aar-primate-protocol.md, src/content/gallery/aar-black-banana.md, src/content/gallery/aar-canopy-breach.md</files>
  <read_first>
    - .planning/phases/01-death-monkeys-site-build/01-CONTEXT.md (D-08 Nakunga/Naku anchor + Gravedigger/Jackdaw; D-09 committed mission-name bit; D-10 specific airsoft roles/loadouts; Claude's Discretion list for dates/locations/photo service)
    - .planning/REQUIREMENTS.md (CONTENT-01/02/03 field requirements; CONTENT-04 = 2-3 realistic entries per collection)
    - src/content.config.ts (the schemas just written — frontmatter MUST satisfy them)
  </read_first>
  <action>
    Create 3 markdown files per collection. Entry IDs derive from filename (no `slug` field). Each file is frontmatter + a short markdown body.

    EVENTS (status spread MUST cover all three enum values for downstream badge testing): include one `upcoming`, one `completed`, one `cancelled`.
    - `op-primate-protocol.md`: title "Op: Primate Protocol", status `upcoming`, future `date` (e.g. 2026-08-15), a fictional `location`, brand-flavored `description`.
    - `black-banana-engagement.md`: title "Black Banana Engagement", status `completed`, past `date` (e.g. 2026-04-12).
    - `op-canopy-breach.md`: title "Op: Canopy Breach", status `cancelled`, a `date`, description noting the scrub.

    ROSTER (D-08/D-10 — active/inactive spread for downstream sort + muted-styling testing): at least one `active: false`.
    - `nakunga.md`: callsign `Nakunga`, realName `Naku`, role + loadout per D-10 specificity (e.g. role "Assault", loadout "HPA rifle / chest rig"), `active: true`, a `joinedDate`.
    - `gravedigger.md`: callsign `Gravedigger`, invented realName, role "Support gunner / LMG platform" style, `active: true`.
    - `jackdaw.md`: callsign `Jackdaw`, role "Recon / DMR", `active: false` (inactive entry for muted styling).

    GALLERY (D-09 AAR flavor; photos use placeholder service per Claude's Discretion — picsum.photos or placeholder.com):
    - `aar-primate-protocol.md`: title, `date`, `eventRef: "op-primate-protocol"`, `summary`, `photos` array of 2-3 placeholder URLs, markdown AAR body.
    - `aar-black-banana.md`: title, `date`, `summary`, `photos`, body.
    - `aar-canopy-breach.md`: title, `date`, `summary`, body (omit `photos` to exercise the optional field).

    Keep dates ISO `YYYY-MM-DD` so `z.coerce.date()` parses without error. Keep content fictional/brand-voiced, not tied to real Badlands history (per D-09).
  </action>
  <verify>
    <automated>cd /home/michael/git/deathmonkeys && test "$(ls src/content/events/*.md | wc -l)" -eq 3 && test "$(ls src/content/roster/*.md | wc -l)" -eq 3 && test "$(ls src/content/gallery/*.md | wc -l)" -eq 3 && grep -rl "status: upcoming" src/content/events && grep -rl "status: cancelled" src/content/events && grep -rl "active: false" src/content/roster && grep -q "callsign: Nakunga" src/content/roster/nakunga.md && echo 'seeds OK'</automated>
    <automated>cd /home/michael/git/deathmonkeys && npx astro sync && grep -q "events" .astro/types.d.ts && grep -q "roster" .astro/types.d.ts && grep -q "gallery" .astro/types.d.ts && npx astro check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 3 `.md` files exist in each of `src/content/events/`, `src/content/roster/`, `src/content/gallery/`.
    - Events seed set includes at least one each of `status: upcoming`, `status: completed`, `status: cancelled`.
    - Roster seed set includes `callsign: Nakunga` with `realName: Naku` AND at least one entry with `active: false`.
    - At least one gallery entry has a `photos:` array and at least one omits `photos`.
    - `npx astro sync` succeeds and `.astro/types.d.ts` references `events`, `roster`, and `gallery`.
    - `npx astro check` exits 0 with no zod coercion errors.
  </acceptance_criteria>
  <done>Nine seed entries satisfy their schemas; sync generates collection types; check passes clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown frontmatter → zod schema | Seed data parsed and type-coerced at build time |
| Markdown body → rendered HTML (later) | Content collection bodies become page HTML in Plans 1.4/1.5 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Content frontmatter injection | mitigate | All collection frontmatter validated by zod schemas in `src/content.config.ts`; malformed values fail `astro check` at build, never reach output. Seed data is author-controlled (no external input). |
| T-02-02 | Information disclosure | Placeholder image URLs leaking real assets | accept | Photos are placeholder-service URLs (picsum.photos/placeholder.com) only — no real team assets per REQUIREMENTS.md out-of-scope. |
| T-02-03 | Tampering | XSS via markdown body rendering | mitigate | Deferred to render sites (Plans 1.4/1.5) which use Astro's default HTML escaping; no `set:html` on untrusted input. All content is first-party author-written. |
</threat_model>

<verification>
- `cd /home/michael/git/deathmonkeys && test -f src/content.config.ts && ! test -f src/content/config.ts` → v5 location correct.
- `cd /home/michael/git/deathmonkeys && npx astro sync` → generates `.astro/types.d.ts` with all three collections.
- `cd /home/michael/git/deathmonkeys && npx astro check` → exits 0, all collection types resolve.
- `ls src/content/{events,roster,gallery}/*.md | wc -l` → 9.
</verification>

<success_criteria>
1. `src/content.config.ts` exists at the root of `src/` (not inside `src/content/`).
2. `astro sync` completes and `.astro/types.d.ts` includes generated types for `events`, `roster`, `gallery`.
3. Each collection directory contains 2–3 `.md` seed files whose frontmatter satisfies the zod schema with no coercion errors.
4. `astro check` passes with all collection types resolving.
</success_criteria>

<output>
Create `.planning/phases/01-death-monkeys-site-build/01-2-SUMMARY.md` when done.
</output>
